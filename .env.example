# OpenAI API key — read implicitly by `new OpenAI()` (the SDK reads
# process.env.OPENAI_API_KEY) in server/api/evaluate.ts, hint.ts,
# question.ts, detect_objectives.ts, detect_objectives_from_text.ts.
OPENAI_API_KEY=your-openai-api-key-here
